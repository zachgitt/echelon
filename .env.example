# Database connection string from Supabase
# Used by: Drizzle ORM for migrations (drizzle.config.ts) and database queries (src/lib/db.ts, db/client.ts)
# For local development, this is automatically provided by Supabase local setup
# For production: Get this from Supabase Dashboard > Project Settings > Database > Connection String (URI format)
DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:54322/postgres

# Supabase Auth Configuration
# Used by: Supabase client utilities for authentication (src/lib/supabase/*)
# For local development: Get these from 'npm run supabase:status' after starting Supabase
# For production: Get these from Supabase Dashboard > Project Settings > API
NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key_here
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here
